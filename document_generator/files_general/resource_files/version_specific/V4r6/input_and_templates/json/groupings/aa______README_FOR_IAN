I am no longer modifying these files; they appear here exactly as they appear when downloaded from the repo.

Note that we can probably automatically download these from github, with the user only needing to input the V#r# info.

ie using a url like:
https://github.com/ECCO-GROUP/ECCO-v4-Configurations/blob/master/ECCOv4%20Release%206/metadata/groupings_for_native_datasets.json

That also relates to a comment I wrote in the config files - those too could be general, and just have any paths and strings
with version info modified at runtime

It would seem, then, that the only piece of "customization" that a user would need to do would be specifiying the granules to download.

